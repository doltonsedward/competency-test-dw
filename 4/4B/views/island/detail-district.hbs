{{> header}}

<div class="container">
    <div class="heading d-flex-responsif">
        <h1>Kabupaten</h1>
        <div class="crud-session">
            <button>Add Provinsi</button>
            <button>Add Kabupaten</button>
        </div>
    </div>
    <div class="container">
        <p></p>
        <div class="wrapper_provinsi">
            <table>
                <tr>
                    <th>Kabupaten</th>
                    <th>Diresmikan</th>
                    <th>Photo</th>
                </tr>
                {{#each island}}
                    <tr>
                        <td>{{this.nama}}</td>
                        <td>{{this.diresmikan}}</td>
                        <td><img src="/uploads/{{this.photo}}" width="200px" alt=""></td>
                    </tr>
                {{/each}}
            </table>
        </div>
        <br>
        <div class="wrapper-btn">
            <button class="btn btn-update"><a href="/pulau/district/update/{{detailId}}">Update</a></button>
            <button class="btn btn-delete"><a href="/pulau/district/delete/{{detailId}}">Delete</a></button>
        </div>
    </div>
</div>

{{> footer}}